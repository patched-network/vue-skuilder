export enum FieldType {
  STRING = 'string',
  NUMBER = 'number',
  INT = 'int',
  IMAGE = 'image',
  MARKDOWN = 'markdown',
  AUDIO = 'audio',
  MIDI = 'midi',
  MEDIA_UPLOADS = 'uploads',
  CHESS_PUZZLE = 'chess_puzzle',
}
